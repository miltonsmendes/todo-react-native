import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        paddingTop: 56,
        marginBottom: 20,
    },
    textPrimary: {
        color: '#fff',
        fontSize: 26,
    },
    textSecundary: {
        color: '#595959',
    },
});