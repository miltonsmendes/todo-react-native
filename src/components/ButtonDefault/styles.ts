import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        borderRadius: 5,
        padding: 10,
        paddingLeft: 20,
        paddingRight: 20,
        marginLeft: 5,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: '#15c115',
    },
    buttonText: {
        color: '#FFF',
        fontSize: 26,
    },
});